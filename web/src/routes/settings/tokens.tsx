import { TokensSettingsBody } from "../../components/settings/TokensPanel";

/** Personal access tokens (moved to a shared panel in spec 34 — also on Profile). */
export function TokensSettingsPage() {
  return <TokensSettingsBody />;
}
